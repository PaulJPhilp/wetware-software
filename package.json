{
  "name": "wetware-software",
  "version": "0.2.0",
  "private": true,
  "packageManager": "bun@1.x",
  "workspaces": [
    "apps/*",
    "packages/*",
    "."
  ],
  "scripts": {
    "dev:web": "cd apps/web && bun run dev",
    "dev:cli": "cd packages/cli && bun run dev",
    "build": "bun run build:all",
    "build:all": "bun run build:shared && bun run build:web && bun run build:cli",
    "build:web": "cd apps/web && bun run prebuild && bun run build",
    "build:cli": "cd packages/cli && bun run build",
    "build:shared": "cd packages/shared && bun run build",
    "test:all": "bun run test:cli",
    "test:cli": "cd packages/cli && bun run test",
    "format": "bunx ultracite fix apps/ packages/",
    "lint:fix": "bunx ultracite fix apps/ packages/",
    "check": "bunx ultracite check apps/ packages/",
    "bootstrap": "bun install"
  },
  "dependencies": {
    "vercel": "^48.12.0"
  },
  "devDependencies": {}
}